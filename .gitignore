# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem

# AI Assistant Tools (local-only, not shared)
.agents/
skills-lock.json
.qoder/
.junie/
.zencoder/
.zenflow/
.qodo/
.qwen/
.commandcode/
.cursor/
.copilot/
.aider/
.claude/
.openclaude/

# pnpm
# pnpm-lock.yaml removed to allow deterministic cloud builds
.pnpm/
.blackboxcli/
# Ignore scripts/ by default but re-include specific operational ones below.
scripts/
!scripts/deploy-api.sh
!scripts/rollback-api.sh
!scripts/setup-secrets.sh
!scripts/sync-monitoring.sh
!scripts/test-routes.sh
!scripts/agent-commerce-ready.mjs
!scripts/agent-buyer.mjs
!apps/web/scripts/test-upstash.mjs

# Secrets (note: .synthesis-credentials is still tracked in git history —
# rotate the key and run: git rm --cached .synthesis-credentials)
.synthesis-credentials

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/

# Deployment (server-specific configs)
deploy/*.conf
deploy/*SUMMARY.md
HETZNER_CONFIG.md
dump.rdb

# Demo videos (local-only, too large for repo; wire in via URL if needed)
apps/web/public/demo/

# test negation
!apps/web/scripts/register-erc8004-agent.mjs

# TestSprite — keep plans/ in repo, ignore temporary artifacts
.testsprite/failure-*
.testsprite/steps-*
.testsprite/failure*/
