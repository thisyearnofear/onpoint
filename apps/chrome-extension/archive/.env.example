## Origin Trial tokens for Chrome Extension context (Chrome Extensions tokens)
# Paste full token strings provided by Chrome Origin Trials for your extension ID
# Only include tokens you actually requested; leave blank to skip.

# Writer API token (Chrome Extensions)
OT_WRITER_TOKEN=""

# Rewriter API token (Chrome Extensions)
OT_REWRITER_TOKEN=""

# Proofreader API token (Chrome Extensions)
OT_PROOFREADER_TOKEN=""

# Prompt API token (Chrome Extensions) — only for Chrome 131–136
OT_PROMPT_TOKEN=""

# Optional: multiple tokens comma-separated (useful for staged rotations)
# OT_EXTRA_TOKENS="token1,token2,token3"